# Full Generated Outputs

These files were produced by rerunning Xu's `generate_validated_engineered_features.py` unchanged against the v0 feature table. They retain all engineered features and QC for traceability and are not the default training entry point.

The orientation comparison selected `direct`. Across 10,634 primary-baseline rows it matched `guide_target_hamming_dist_computed` exactly, while agreement with the raw `guide_target_hamming_dist_raw` was 91.593%. This does not resolve the raw Hamming field's meaning or independently confirm biological orientation.
