# EasyDesign 评估结果

本目录保存组员结果和参考评估产物，不保存实验标签，也不作为默认训练数据入口。

- `xu_first_run/`：徐同学提供的首轮特征重要性结果。
- `reference_improved_feature_run/`：用户指定用于参考的改进结果，包括重要性、逐行预测、指标和图。

评估文件必须与生成它们的代码、split、参数和随机种子一起复现后，才能作为项目确认性能。
