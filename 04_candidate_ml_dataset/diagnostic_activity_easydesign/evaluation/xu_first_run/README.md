# 徐同学首轮结果

`first_run_feature_importance_top30.csv` 为徐同学提供的首轮特征重要性结果，SHA-256 为 `e2db29c8525f1910ac3e662f8c0e8eb92fc4889215df7c7a90f114a4691724ea`。

文件名含 `top30`，但实际只有 5 行特征：计算 Hamming、原始 Hamming、target length、crRNA GC 和 target GC。结果被原样保留。由于缺少对应训练代码和指标文件，本轮只把它作为历史结果与特征选择证据，不声称可独立复现。
