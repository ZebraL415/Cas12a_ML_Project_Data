# 修正错配贡献审查中的贡献者姓名

## 本轮范围

本轮仅修正 EasyDesign 错配贡献审查中对两位贡献者的称呼，不改变数据、统计值、判断或处理路线。

## 修改规则

- 中文：`Xu 同学` / `Xu` 改为“诗睿同学”或“诗睿”。
- 中文：`林同学` / `林` 改为“可恩同学”或“可恩”。
- 英文：首次使用 `Shirui (诗睿)` 和 `Ke'en (可恩)`，后文使用 `Shirui` 和 `Ke'en`。
- 历史 Git commit 标题 `Integrate Xu engineered features` 是不可改写的提交原文，因此保持不变。

## 修改文件

- `mismatch_contribution_review_zh.md`
- `mismatch_contribution_review_en.md`

## 边界

未修改 `01_raw/`、CSV、Excel、脚本、模型输入或任何审计结论。
